name: .Net Build

on:
    push:

jobs:

 build:
    name: 🛠️ Build
    runs-on: ubuntu-latest

    steps:
        - name: Checkout
          uses: actions/checkout@v4
          with:
            path: main
        - name: Dotnet Build
          uses: EasyDesk/action-dotnet-build@v1
          with:
            path: ./github4women/